load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "unit",
    srcs = glob([
        "*.cc",
        "*.h",
    ]),
    copts = ["-Iexternal/googletest/include"],
    linkstatic = True,
    deps = [
        "//core",
        "@googletest//:gtest_main",
    ],
)
