load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "unit",
    srcs = glob(["*.cc"]),
    copts = ["-Iexternal/googletest/include"],
    linkstatic = True,
    deps = [
        "//util",
        "@googletest//:gtest_main",
    ],
)
